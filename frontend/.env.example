# Backend API URL for production
# Leave empty to use default: /api (relative URL for same-origin requests)
# For separate domain deployment, use full URL: https://api.example.com/api
VITE_API_URL=http://localhost:5000/api
